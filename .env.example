# All environment variables are optional — the app runs with sensible defaults.
# Copy this file to `.env.local` only if you need to override something.

# Base URL for the API. Leave unset to use this app's own /api routes
# (same-origin). Set it only to point the UI at a different backend host.
# NEXT_PUBLIC_API_URL=
